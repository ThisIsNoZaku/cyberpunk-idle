import * as React from "react";
import * as d3 from "d3";

export default function Jobs(props) {
    //const nodes = props.jobs.get().map(n => Object.create(n));
    // const simulation = d3.forceSimulation(nodes)
    //     .force()
    return <div></div>
}